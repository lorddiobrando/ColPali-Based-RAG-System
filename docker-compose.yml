version: "3.9"
services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./:/workspace
    working_dir: /workspace
    command: flask run --host=0.0.0.0 --port=5000
